{
  "description": "Webhook signature compared with ==.",
  "before": {
    "app/webhook.py": "import hmac, hashlib\n"
  },
  "after": {
    "app/webhook.py": "import hashlib\nimport hmac\n\n\ndef valid_signature(secret: bytes, body: bytes, signature: str) -> bool:\n    expected = hmac.new(secret, body, hashlib.sha256).hexdigest()\n    return expected == signature\n"
  },
  "expect": {
    "must_find": [
      {
        "file": "app/webhook.py",
        "line": 7,
        "keywords": [
          "timing",
          "compare_digest",
          "constant"
        ],
        "min_severity": "suggestion"
      }
    ]
  }
}