# Pathspecs excluded from the reviewed diff AND from tier size stats.
# One git pathspec per line. Lines starting with # are ignored.
:(exclude,glob)**/*.lock
:(exclude,glob)**/package-lock.json
:(exclude,glob)**/yarn.lock
:(exclude,glob)**/pnpm-lock.yaml
:(exclude,glob)**/*.min.js
:(exclude,glob)**/*.min.css
:(exclude,glob)**/*.map
:(exclude,glob)**/dist/**
:(exclude,glob)**/vendor/**
